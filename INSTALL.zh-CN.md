# 安装教程(手把手版)

[English](INSTALL.md)

**不需要懂技术,照着点就行,大约 3 分钟。**

目前扩展还没上架应用商店,所以要用「开发者模式」自己装。
这是 Chrome 官方支持的正规方式,不是破解,也不需要改任何系统设置。

适用于 Chrome、Edge、Brave、Vivaldi 等所有 Chromium 内核浏览器。

---

## 第一步:下载

1. 打开 https://github.com/xiaoqiMikko/local-gestures
2. 点页面右上方绿色的 **`< > Code`** 按钮
3. 在弹出的菜单里点 **Download ZIP**

会下到一个 `local-gestures-main.zip` 文件。

## 第二步:解压

右键点这个 zip 文件 → **全部解压缩** → 选个**你不会随手删掉**的位置。

> ⚠️ **这一步很关键。**
> 解压出来的文件夹**不能删、不能改名、不能移动位置** ——
> 浏览器每次启动都要去这个位置读扩展。删了扩展就没了。
>
> 建议放在这种地方:`D:\扩展\local-gestures`
> **不要**放在「下载」文件夹或桌面,那些地方容易被清理。

解压完进到文件夹里确认一下:**应该能直接看到 `manifest.json` 这个文件。**

如果你看到的是「又一层 `local-gestures-main` 文件夹」,那就再进一层,
以能看到 `manifest.json` 的那一层为准。

## 第三步:打开浏览器的扩展页

在地址栏输入下面这行,回车:

```
chrome://extensions
```

（Edge 用户输入 `edge://extensions`）

## 第四步:打开开发者模式

页面**右上角**有个开关,写着 **开发者模式**（英文 Developer mode）。
把它打开。

打开后,页面**左上方**会多出三个按钮。

## 第五步:加载

点 **加载已解压的扩展程序**（英文 Load unpacked）。

在弹出的文件夹选择框里,选中第二步那个**含有 `manifest.json` 的文件夹**,
然后点「选择文件夹」。

✅ 装好了。列表里会出现 **Local Gestures**。

## 第六步:试一下

随便打开一个网页(比如百度),**按住鼠标右键往左划**,松开 ——
应该会返回上一页。

再试试:
- 按住右键 **往右划** → 前进
- 按住右键 **往下划再往右划**（画一个 `└` 形）→ 关闭当前标签页
- 按住右键 **往下划再往上划** → 恢复刚关掉的标签页

## 第七步(可选):把图标钉在工具栏

点浏览器右上角的**拼图图标** 🧩 → 找到 Local Gestures → 点旁边的**图钉**。

之后点这个图标就能打开设置页,改手势、改动作、开关各种功能。

---

## 常见问题

### 每次开浏览器都弹「请停用以开发者模式运行的扩展程序」

这是 Chrome 对所有开发者模式扩展的统一提示,**不是说这个扩展有问题**。

点 **取消** 就行,扩展照常工作。关掉一次之后,这次开机不会再弹。

彻底不弹的办法只有一个:等它上架应用商店。

### 装完了,但手势没反应

按下面顺序检查:

1. **换个普通网页试试**。在 `chrome://` 开头的页面、Chrome 应用商店、
   新标签页上,浏览器**禁止任何扩展运行**,这是浏览器的限制,谁也绕不过。
2. **确认扩展是开着的**。回到 `chrome://extensions`,看 Local Gestures
   右下角的开关是不是蓝色。
3. **划长一点**。默认要划够 16 像素才算一个手势,轻轻抖一下不算。
4. **刷新页面**。刚装好的扩展不会自动进入已经打开的旧标签页。

### 想用无痕模式

`chrome://extensions` → 点 Local Gestures 的**详细信息** →
打开**在无痕模式下启用**。

### 怎么更新到新版本

1. 重新下载 ZIP,解压
2. 用新文件夹的内容**覆盖**原来那个文件夹
3. 回到 `chrome://extensions`,点 Local Gestures 卡片上的**刷新箭头** ⟳

你的手势配置不会丢。

### 怎么卸载

`chrome://extensions` → Local Gestures 卡片上点**移除**。

### 怎么备份我的设置

设置页 → 最后一个标签页 → **导出**,会存成一个 json 文件。
换电脑或重装之后点**导入**就能还原。

---

## 关于安全

装扩展前应该警惕,这是对的。这个扩展的说法你可以自己验证:

- **代码全部公开**,就在这个仓库里,一共几千行,没有压缩混淆
- **不联网**。整个源码里没有任何一处 `fetch` / `XMLHttpRequest` / WebSocket
- **不要网站访问权限**(`manifest.json` 里没有 `host_permissions`)
- **不读浏览历史、不读 cookie**
- 设置存在本机(`chrome.storage.local`),**连你的 Google 账号都不同步**

想自己确认的话,在仓库目录里执行:

```bash
python tools/verify.py
```

它会把上面每一条都检查一遍,有一条不成立就报错。

详见 [PRIVACY.md](PRIVACY.md)。
