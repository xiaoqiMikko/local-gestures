# 需要人工确认的检查项

`tools/e2e_test.py` 覆盖了 45 个动作里的 43 个和全部输入方式。
剩下这些**自动化确实测不了**,发版前请人工点一遍。

> 之所以单独列出来而不是悄悄跳过:一个没被验证过的功能,和一个验证通过的功能,
> 在测试报告里长得一模一样 —— 除非把差别写清楚。

## 怎么装上去手动测

Chrome 137 之后**移除了 `--load-extension` 命令行开关**,自动化脚本无法再自己装扩展。
手动装一次:

1. 打开 `chrome://extensions`
2. 打开右上角**开发者模式**
3. 点**加载已解压的扩展程序**,选中本仓库目录

---

## 清单

### 1. 新建无痕窗口(`newIncognito`)

**为什么测不了**:创建隐身窗口会让 Playwright 失去 target 追踪,
它随即会杀掉自己启动的浏览器 —— 整个测试套件跟着结束。

**怎么测**:
- 到设置页 → 手势,把某个手势(比如 `↑←`)的动作改成「新建无痕窗口」
- 在任意网页上画这个手势

**期望**:弹出一个无痕窗口,**原来的浏览器一切正常**,没有崩溃或退出。

**如果失败**:记下来,这是必须修的 bug。

---

### 2. 打印(`print`)

**为什么测不了**:会弹出系统打印对话框并阻塞渲染进程,自动化关不掉它。

**怎么测**:把某个手势绑到「打印」,在网页上画出来。

**期望**:弹出 Chrome 的打印预览。按 Esc 关掉后页面一切正常。

---

### 3. 键盘快捷键真的按下去

**为什么测不了**:`chrome.commands` 的快捷键由浏览器进程处理,
不经过页面,自动化按键送不到那一层。

自动化**已验证**:四个命令已注册、都有默认组合、每个槽位都有默认动作。
**未验证**:真的按下 `Alt+Shift+1` 会不会执行。

**怎么测**:按一下 `Alt+Shift+1`。

**期望**:恢复最近关闭的标签页(默认绑定)。

---

### 4. 右键菜单真的能点

**为什么测不了**:右键菜单是操作系统的原生控件,不在页面里,自动化点不到。

自动化**已验证**:菜单构建过程不报错(开启态和关闭态都测了)。
**未验证**:菜单项真的出现、点了真的执行。

**怎么测**:
- 设置页 → 其他入口 → 打开「把动作加进右键菜单」
- 在网页上点右键

**期望**:菜单里出现 "Local Gestures" 子菜单,里面是配置的那几项;点击能执行。

---

### 5. 工具栏图标点击

**为什么测不了**:`chrome.action.onClicked` 同样无法用程序触发。

**怎么测**:点一下工具栏上的扩展图标。

**期望**:打开设置页(默认动作)。改成别的动作后再点,应执行新动作。

---

### 6. 可选权限的授权弹窗

**为什么测不了**:权限确认框是浏览器原生 UI。

自动化**已验证**:默认没有授予 `downloads` / `bookmarks`;缺权限时动作会
返回明确错误,并且**页面上会弹出提示告诉用户去哪开**。
**未验证**:点下去之后授权流程本身。

**怎么测**:
- 设置页 → 高级 → 勾选「下载」
- 应弹出 Chrome 的权限确认框,点允许
- 再把某个拖拽方向设成「下载」,拖一张图片试试

**期望**:授权后能下载;回到设置页取消勾选能撤销权限。

---

### 7. 触摸手势(在真的触摸屏上)

自动化用 CDP 合成的触摸事件**已验证**通过,但那不等于真手指。

**怎么测**:有触摸屏的话,设置页 → 其他入口 → 打开触摸手势,双指划一下。

---

## 一条给自己的提醒

⚠️ **不要用 Playwright 自带的 Chromium 判断任何跟渲染有关的事。**

在部分机器上它启动时不加载浏览器默认样式表,所有块级元素都变成
`display: inline` —— 表格塌成一行,`<style>` 标签内容被当正文画出来。
页面看着全坏,换系统 Chrome 一切正常。

判定方法(不加载任何扩展也能复现):

```js
getComputedStyle(document.body).display   // 正常应为 "block"
```
